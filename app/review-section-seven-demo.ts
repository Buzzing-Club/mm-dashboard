import type { ReviewMarketInput, SectionSevenData } from "./review-section-seven-data";

// Preview never substitutes demo observations for missing phase-level measurements.
export function buildSectionSevenDemo(_market: ReviewMarketInput, _markets: ReviewMarketInput[]): SectionSevenData | null {
  void _market;
  void _markets;
  return null;
}
